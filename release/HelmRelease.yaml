apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helmapp
  namespace: flux-system
spec:
  chart:
    spec:
      chart: ./flux-chart
      sourceRef:
        kind: GitRepository
        name: react-app
        namespace: default
  interval: 1m0s
  targetNamespace: helmspace
