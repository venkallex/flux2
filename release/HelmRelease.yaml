apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helmapp
  namespace: flux-system
spec:
  chart:
    spec:
      chart: flux-chart
      path: ./flux-chart
      sourceRef:
        kind: GitRepository
        name: react-app
  interval: 1m0s

